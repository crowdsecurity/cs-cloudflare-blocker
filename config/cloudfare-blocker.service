[Unit]
Description=The crowdsec cloudfare connector
After=syslog.target network.target remote-fs.target nss-lookup.target

[Service]
Type=forking
#PIDFile=${PID}/crowdsec-cloudfare.pid
ExecStart=${BIN} -c ${CFG}/cloudfare-blocker.yaml
ExecStartPost=/bin/sleep 0.1

[Install]
WantedBy=multi-user.target


